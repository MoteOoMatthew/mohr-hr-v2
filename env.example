# MOHR HR System V2 - Environment Configuration

# Server Configuration
NODE_ENV=development
PORT=5000
HOST=0.0.0.0

# Security
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
SESSION_SECRET=your-session-secret-key-change-this-in-production

# Database
DB_TYPE=sqlite
DB_PATH=./backend/database/mohr_hr_v2.db

# CORS Configuration
CORS_ORIGIN=*

# Frontend URL (for OAuth redirects)
FRONTEND_URL=http://localhost:3000

# Google OAuth Configuration
# Get these from Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:5000/auth/google/callback

# Google API Scopes (comma-separated)
GOOGLE_SCOPES=profile,email,https://www.googleapis.com/auth/calendar,https://www.googleapis.com/auth/drive.readonly

# Production Settings (for Render deployment)
# These will be set automatically by Render
# NODE_ENV=production
# PORT=10000 (Render sets this automatically)
# GOOGLE_CALLBACK_URL=https://your-app-name.onrender.com/auth/google/callback
# FRONTEND_URL=https://your-frontend-domain.com 